## 1. Project Setup
- [ ] 1.1 Initialize SvelteKit project with TypeScript (if not already done)
- [ ] 1.2 Set up Railway.app deployment configuration (railway.json or railway.toml)
- [ ] 1.3 Install dependencies: `ws@^8.18.0` and `@types/ws@^8.5.0`
- [ ] 1.4 Configure environment variables (create `.env.example` with PORT, NODE_ENV)
- [ ] 1.5 Set up backend project structure:
  - [ ] Create `/lib/streams` directory
  - [ ] Create `/lib/scenarios` directory
  - [ ] Create `/lib/config` directory
  - [ ] Create `/lib/data` directory
  - [ ] Create `/routes/api/simulation` directory
  - [ ] Create `/routes/api/streams` directory
  - [ ] Create `/routes/admin` directory

