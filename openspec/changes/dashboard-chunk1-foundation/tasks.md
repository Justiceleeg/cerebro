## 1. Project Setup (Phase 1.1)
- [x] 1.1 Verify SvelteKit project is initialized with TypeScript (already done with simulator)
- [x] 1.2 Install frontend dependencies:
  - [x] Install `echarts@^5.5.0` (installed 5.6.0)
  - [x] Install `@ai-sdk/openai@^2.0.64` (already installed)
  - [x] Install `ai@^5.0.0` (already installed)
  - [x] Install `shadcn-svelte` (installed 1.0.11)
  - [x] Install `lucide-svelte` (already installed)
- [x] 1.3 Set up frontend project structure:
  - [x] Create `/routes/dashboard` directory
  - [x] Create `/lib/components/dashboard` directory
  - [x] Create `/lib/components/ui` directory (already exists)
  - [x] Create `/lib/stores` directory
  - [x] Create `/lib/ai-contextualizer` directory
  - [x] Create `/lib/utils` directory (already exists)
- [x] 1.4 Initialize shadcn-svelte components (already configured via components.json)

## 2. State Management Setup (Phase 1.2)
- [x] 2.1 Create WebSocket connection store (`lib/stores/websocket.ts`):
  - [x] Connection state (connected/disconnected/reconnecting)
  - [x] Subscription management (topics array)
  - [x] Reconnection logic
  - [x] Error handling
- [x] 2.2 Create stream data store (`lib/stores/streams.ts`):
  - [x] Stream events aggregation (Map<streamName, events[]>)
  - [x] Historical data storage
  - [x] Real-time update logic
  - [x] Normalized value calculations
- [x] 2.3 Create UI state store (`lib/stores/ui.ts`):
  - [x] Filter state (status, domain, event types)
  - [x] Selection state (selected streams, selected events)
  - [x] Time range state (start, end, preset)
- [x] 2.4 Create chart state store (`lib/stores/chart.ts`):
  - [x] Zoom level state
  - [x] Pan position state
  - [x] Selected streams for chart display
  - [x] Event markers visibility
- [x] 2.5 Create recommendations store (`lib/stores/recommendations.ts`):
  - [x] Recommendations array (active, resolved)
  - [x] Priority filtering
  - [x] Action status tracking

## 3. Type Definitions (Phase 1.3)
- [x] 3.1 Import and re-export backend type definitions from `src/lib/types/core.ts` (matching `docs/API_CONTRACT.md`):
  - [x] Import `StreamEvent` from `$lib/types/core`
  - [x] Import `ExternalEvent` from `$lib/types/core`
  - [x] Import `ScenarioModifier` from `$lib/types/core`
  - [x] Import `StreamBaseline` from `$lib/types/core`
  - [x] Import `SimulationState` from `$lib/types/core`
  - [x] Import `StreamModification` from `$lib/types/core`
  - [x] Import `CascadeRule` from `$lib/types/core`
  - [x] Verify all types match `docs/API_CONTRACT.md` exactly
- [x] 3.2 Create dashboard-specific types:
  - [x] `HeatmapCell` interface (stream name, normalized value, status, domain)
  - [x] `Recommendation` interface (priority, title, description, actions, confidence)
  - [x] `ChartSeries` interface (stream name, data points, color)
  - [x] `ChartDataPoint` interface (timestamp, value, normalized)
  - [x] `WebSocketMessage` union type (subscribe, event, batch, error, etc.)
  - [x] `FilterState` interface (status, domain, event types, time range)
  - [x] `TimeRange` interface (start, end, preset)
- [x] 3.3 Create utility types:
  - [x] `StreamName` type (union of all stream names)
  - [x] `EventType` type (union of event types)
  - [x] `Priority` type (union of priority levels)
- [x] 3.4 Export all types from `lib/types/dashboard.ts`
- [x] 3.5 Update `lib/types/index.ts` to export dashboard types

## 4. Minimal Dashboard Page (Slice 1)
- [x] 4.1 Create `/routes/dashboard/+page.svelte` (main operations dashboard):
  - [x] Basic layout with header
  - [x] Placeholder sections for chart, heatmap, events, recommendations
  - [x] Hardcoded connection status: "Connected"
  - [x] Basic styling with Tailwind CSS (shadcn-svelte components available for future use)
- [ ] 4.2 (Optional) Update root `/` route:
  - [ ] Option A: Redirect to `/dashboard`
  - [ ] Option B: Simple landing page with links to `/dashboard` and `/admin`
  - [x] Option C: Leave as-is (default SvelteKit welcome page) - **Selected**
- [x] 4.3 Test dashboard route:
  - [x] Navigate to `/dashboard`, verify page loads (fixed rune issue by converting stores to `.svelte.ts`)
  - [x] Verify layout structure is correct
  - [x] Verify connection status displays
  - [x] Note: `/admin` route already exists and works (no testing needed)

